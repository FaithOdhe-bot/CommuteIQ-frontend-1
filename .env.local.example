# Copy this file to .env.local and fill in your backend URL once it's deployed.
# Leave it unset (or delete .env.local) and the app runs on mock data automatically.
VITE_API_URL=https://smartcommute-api.onrender.com
